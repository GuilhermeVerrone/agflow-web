NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Slug do tenant desta instalação frontend.
# O frontend usa isso para buscar o tenantId (UUID) automaticamente
# via GET /tenants/slug/:slug antes do primeiro login.
# O usuário NUNCA precisa informar o tenant — só email + senha.
NEXT_PUBLIC_TENANT_SLUG=agflow-test
